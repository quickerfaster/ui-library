review your implementation and make sure that there is no violation of the libarary's philosphy 1) the library must be decoupled completely from the consuming app. but the consuming app can depend on the library. 2) the consuming app must be self content in such a way that all the releted files (models, midrations, services, components, views etc.) to a given module must be housed inside that module in such a way that any fresh laravel project that has the ui library can copy any module into its app/Modules/ folder and have the module pluged and played read. you may need to check the relevant docs for more detail.